# Architecture

## 1. High-Level Overview

This repository is a **static website** for a Russian-language knowledge base ("TokenBel Wiki"), not a service or application. It contains no server runtime code: the deliverable is a static `public/` directory generated by Hugo and served verbatim by a Cloudflare Worker configured with Static Assets. There is no Worker script, no KV namespace, no bindings, and no origin server (`wrangler.toml`, `docs/deployment.md`).

The architecture is a classic static-site-generator pipeline. Markdown content in `content/` is combined with local Hugo templates in `layouts/` and a standalone Tailwind CSS 4 source (`static/css/input.css`) under the configuration in `hugo.yaml`, producing a self-contained set of HTML/CSS/asset files. All presentation logic is local — there is no external Hugo theme and no Sass/SCSS dependency, which is why the Hugo **standard** edition is intentionally used instead of Extended (`docs/deployment.md`).

Two distinct build entrypoints coexist and target different environments. Local development and validation run Hugo inside the `hugomods/hugo:0.164.0` Docker image via the `Makefile`. Cloudflare production builds run `build.sh`, which downloads pinned Hugo and Node binaries, verifies their SHA-256 checksums, and is deliberately restricted to Linux x86_64 (`build.sh`, `Makefile`).

The runtime model is request-to-static-asset: an incoming request is resolved by the Cloudflare Worker against the deployed `public/` directory, and any unmatched path falls back to the nearest Hugo `404.html` with HTTP 404 (`wrangler.toml` `not_found_handling = "404-page"`).

Unknowns:
- `wrangler.toml` declares a zone route `wi.tokenbel.info/*` and a custom domain `wi.tokenbel.info`, while `hugo.yaml` `baseURL` and the canonical `<link>` are `https://wiki.tokenbel.info/`. `docs/deployment.md` documents this as an intentional staging/cutover state pending a separately approved DNS change; the production hostname `wiki.tokenbel.info` is not yet wired to this Worker by repository configuration alone.

## 2. System Architecture (Logical)

### Content Layer

- Responsibility: Authoring surface — Markdown articles and section landing pages, including front matter that drives navigation, icons, dates, and SEO.
- Code locations: `content/`, `archetypes/default.md`.
- Entry points: `content/_index.md` (home), `content/<section>/_index.md` (section landing), `content/<section>/<slug>/index.md` (leaf-bundle article).
- Depends on: Site Configuration (consumes `menus.main`, `params`, locale).
- Must not depend on: Templates or CSS directly; the contract is data-only via front matter.
- Owns: Page titles, descriptions, `weight`, `icon` enum, `date`/`lastmod`, hero parameters.
- State and external boundaries: `enableGitInfo: false` (`hugo.yaml`) means dates come exclusively from front matter, never from Git.
- Evidence: `content/AGENTS.md`, `content/_index.md`, `hugo.yaml`.

### Presentation Layer

- Responsibility: All HTML structure, SEO meta, and styling. Pure presentation — no business logic.
- Code locations: `layouts/` (`baseof.html`, `home.html`, `404.html`, `_default/list.html`, `_default/single.html`, `_markup/render-table.html`, `partials/*`), `static/css/input.css`.
- Entry points: `layouts/_default/baseof.html` (page skeleton), `layouts/partials/head.html` (title, canonical, robots, OG, JSON-LD), `layouts/home.html` (home composition).
- Depends on: Content Layer front matter (reads `.Params.heroPrimary*`, `icon`, `excludeFromRecent`), Site Configuration (`site.Params.*`, `site.BaseURL`).
- Must not depend on: External Hugo themes, Dart Sass / `.scss`, JavaScript frameworks (`layouts/AGENTS.md`, `docs/deployment.md`).
- Owns: Canonical URL emission, `noindex,follow` for 404, OpenGraph/JSON-LD, and the static CSS link.
- State and external boundaries: pinned `@tailwindcss/cli` (`package.json` `css:build`) compiles two committed outputs — `static/css/output.css` (unminified) and `static/css/tailwind.min.css` (minified). `layouts/partials/css.html` links **only** `tailwind.min.css` (hardcoded, no environment switch). `make css-check` diffs both against fresh CLI builds to detect staleness. Inferred: `output.css` is retained as a `css:watch`/freshness reference and is not referenced by any template.
- Evidence: `layouts/AGENTS.md`, `layouts/partials/css.html`, `layouts/partials/head.html`, `layouts/home.html`, `package.json`.

### Site Configuration & Static Assets

- Responsibility: Global site identity, navigation, external link catalog, and immutable files served as-is.
- Code locations: `hugo.yaml`, `static/` (`favicon.svg`, `wiki_logo.svg`, `wiki_logo.webp`, `css/`).
- Entry points: `hugo.yaml` (`baseURL`, `languages.ru`, `taxonomies`, `menus.main`, `params.*`).
- Depends on: Nothing (root configuration).
- Owns: Canonical domain `https://wiki.tokenbel.info/`, section menu order, dashboard/tokens/market external URLs.
- Evidence: `hugo.yaml`, `static/`.

### Build Toolchain

- Responsibility: Reproducible generation of `public/` from the layers above. Two parallel paths for two environments.
- Code locations: `Makefile` (Docker/local path and CSS regeneration), `build.sh` (pinned Cloudflare path), `package.json` (Tailwind CLI and Wrangler orchestration).
- Entry points: `make css-build` / `make css-watch` / `make dev` / `make build` / `make check` (Docker, `hugomods/hugo:0.164.0`); `./build.sh` (native pinned Hugo 0.164.0 + Node 24.18.1, Linux x86_64 only); `make cloudflare-build` / `make deploy` wrap the package scripts.
- Depends on: Content, Presentation, Site Configuration layers (consumes them as Hugo inputs).
- Must not depend on: Extended Hugo features, unpinned tools (`build.sh` pins and checksum-verifies all binaries).
- Owns: The `public/` artifact, `resources/`, `.cache/` (all build outputs, git-ignored per `.gitignore`).
- Evidence: `Makefile`, `build.sh`, `package.json`, `.gitignore`.

### Cloudflare Deployment Target

- Responsibility: Deliver the built `public/` as a Worker with Static Assets. Configuration-only — no code.
- Code locations: `wrangler.toml`, `docs/deployment.md`.
- Entry points: `wrangler deploy` (`npm run deploy`, orchestrated by `build.sh` + `npm run deploy` in Workers Builds).
- Depends on: Build Toolchain output (`public/`, via `[assets] directory = "./public"`).
- Owns: Worker name `tokenbel-wiki`, `not_found_handling`, routes, observability logs/traces, smart placement.
- Evidence: `wrangler.toml`, `docs/deployment.md`.

### Dependency direction

```text
hugo.yaml ──┐
content/ ───┼──► Hugo (standard ed.) ──► public/ ──► Wrangler ──► Cloudflare Worker
layouts/ ───┤        ▲                       │
static/  ───┘        │                       └─► 404.html fallback (HTTP 404)
              CSS: @tailwindcss/cli → committed static output (only tailwind.min.css is linked)
```

Direction is strictly left-to-right: content/templates never import build tooling, and build output never feeds back into sources. `build.sh` and the `Makefile` are alternative front-ends to the same Hugo build; they do not chain into each other.

## 3. Code Map (Physical)

```text
content/            Markdown + front matter; auto-assembled sections (news, statistics, guides, policies, about)
archetypes/         `hugo new` template (`default.md`)
layouts/            Local Hugo templates only — no theme
  _default/         baseof.html (skeleton), list.html (section), single.html (article)
  _markup/          render-table.html (Markdown table render hook)
  partials/         head.html (SEO + canonical/robots), css.html (links tailwind.min.css), header/footer, page-dates, section-card (icon enum), recent-pages (excludeFromRecent)
  home.html         home composition (hero + site.Sections + recent + useful links)
  404.html          fixed 404 copy and actions
static/css/         input.css source; output.css and tailwind.min.css committed CLI output
static/             immutable files served verbatim (CSS, favicon.svg, wiki_logo.*)
hugo.yaml           site config: baseURL, ru locale, menus, params, taxonomies
Makefile            Docker-based local dev/build/check
build.sh            pinned Cloudflare build (Linux x86_64, checksum-verified Hugo+Node)
package.json        Tailwind CLI and Wrangler scripts
wrangler.toml       Cloudflare Worker Static Assets config
docs/deployment.md  deployment, staging, rollback, pinned-tool rationale
```

Generated/build areas excluded from the model: `public/`, `resources/`, `.cache/`, `.wrangler/`, `node_modules/`, `.hugo_build.lock` (all git-ignored by `.gitignore`). `assets/` and `scripts/` are reserved and empty.

## 4. Life of a Request / Primary Data Flow

### Build flow — sources to static site

1. Trigger: `make build` (local) or `./build.sh` (Cloudflare).
2. Entry point: Hugo CLI inside Docker (`Makefile`) or the pinned native binary (`build.sh`).
3. Coordination: Hugo reads `hugo.yaml`, discovers `content/` sections, resolves templates from `layouts/`.
4. Core or domain processing: Markdown rendered to HTML; `site.Sections.ByWeight` drives home/section lists; `.RegularPages.ByLastmod.Reverse` orders article lists; `recent-pages.html` filters out pages with `excludeFromRecent`; `css.html` links the committed `tailwind.min.css`.
5. Persistence or external interaction: Files written to `public/` (`--gc --minify --environment production`); no network calls during build.
6. Output or side effect: Self-contained `public/` directory with `tailwind.min.css` and a root `404.html`.

Architectural boundaries crossed:
- Content front matter → template rendering (data contract only).
- `static/css/input.css` → pinned Tailwind CLI → committed `static/css/tailwind.min.css` → Hugo static copy.

Evidence:
- `Makefile`, `build.sh`, `layouts/partials/head.html`, `layouts/home.html`, `layouts/_default/list.html`, `layouts/partials/recent-pages.html`.

### Request flow — runtime serving at Cloudflare

1. Trigger: HTTP request to the Worker hostname.
2. Entry point: Cloudflare Worker with Static Assets, asset root `./public` (`wrangler.toml`).
3. Coordination: Cloudflare resolves the path against the deployed static files.
4. Core or domain processing: None — pure static asset match; no Worker script executes.
5. Persistence or external interaction: None at runtime.
6. Output or side effect: Matched file (200) or nearest `404.html` with HTTP 404 (`not_found_handling = "404-page"`); canonical `<link>` always points at `https://wiki.tokenbel.info/`.

Architectural boundaries crossed:
- Build artifact (`public/`) → edge runtime (immutable handoff).

Evidence:
- `wrangler.toml`, `layouts/partials/head.html`, `docs/deployment.md`.

## 5. Architectural Invariants & Constraints

- Rule: No external Hugo theme and no Dart Sass / `.scss`; Tailwind CSS 4 uses a single plain-CSS source.
- Rationale: Keeps the Hugo **standard** edition while preserving a reproducible CSS build.
- Enforcement / Signals: `layouts/AGENTS.md`, `package.json`; absence of `theme`/`themes/` and `.scss` files; `make css-build` regenerates committed static output and `make check` detects stale CSS by diffing both committed files against fresh CLI builds.

- Rule: Page dates come from front matter, never Git.
- Rationale: Decouples rendered dates from commit history.
- Enforcement / Signals: `enableGitInfo: false` in `hugo.yaml`; `.Lastmod` resolved from front matter in `layouts/partials/page-dates.html`.

- Rule: The canonical domain is fixed at `https://wiki.tokenbel.info/`.
- Rationale: SEO consistency across local, staging, and production.
- Enforcement / Signals: `baseURL` in `hugo.yaml`; `.Permalink` canonical/OG emission in `layouts/partials/head.html`; `layouts/AGENTS.md` prohibits `localhost`/port in canonical.

- Rule: The 404 page must emit `noindex, follow` and the literal text `Страница не найдена`; the home must contain `База знаний TokenBel`.
- Rationale: Required SEO/copy contracts enforced by CI-equivalent validation.
- Enforcement / Signals: `make check` greps `public/index.html` and `public/404.html`; `layouts/404.html`, `layouts/partials/head.html`, `content/_index.md`.

- Rule: Sections are auto-assembled from `content/`; menu order is declared in `hugo.yaml`.
- Rationale: Single source of truth for navigation.
- Enforcement / Signals: `home.html` iterates `site.Sections.ByWeight`; `menus.main` in `hugo.yaml`.

- Rule: The deployed unit is static only — no Worker script, bindings, or KV.
- Rationale: Operational simplicity and zero runtime cost.
- Enforcement / Signals: `wrangler.toml` has no `main`, bindings, or secrets; `docs/deployment.md`.

- Rule: `public/` is the sole deployment input.
- Rationale: Cloudflare serves exactly what Hugo produced.
- Enforcement / Signals: `[assets] directory = "./public"` in `wrangler.toml`; `.gitignore` excludes `public/`, `resources/`, `.cache/`, `.wrangler/`.

- Rule: Cloudflare builds use pinned, checksum-verified tooling and run only on Linux x86_64.
- Rationale: Reproducible production builds.
- Enforcement / Signals: `build.sh` (`require_linux_x64`, SHA-256 checks, pinned Hugo 0.164.0 / Node 24.18.1 / Wrangler 4.118.0 via `package.json`); `docs/deployment.md`.

- Rule: The section `icon` enum and the home `hero*` parameters are a data contract between `content/_index.md` / section `_index.md` and specific templates.
- Rationale: Unknown icon values silently fall back to `info`; missing hero params break the home layout.
- Enforcement / Signals: `layouts/partials/section-card.html` (`dict` over `{news, chart, guide, document, info}` with `info` fallback), `layouts/home.html`, `content/AGENTS.md`. Inferred: enforcement is by convention — no build-time validator exists.

## 6. Documentation Strategy

This `ARCHITECTURE.md` owns the global architecture map: layers, dependency direction, representative build/runtime flows, and architectural invariants. It does not duplicate task-specific operating rules.

- `/AGENTS.md` — repository-wide agent operating rules and task-based context routing (referenced, not copied here).
- `content/AGENTS.md` — local rules for editing `content/` and front matter contracts.
- `layouts/AGENTS.md` — local rules for templates, SEO meta, and the CSS pipeline.
- `docs/deployment.md` — authoritative deployment runbook: pinned tools, Workers Builds setup, staging, cutover, rollback.
- `README.md` — project overview, local-run instructions, and contributor structure guide.

No ADRs, package READMEs, schema docs, or `DESIGN.md` exist in the repository; their absence does not limit architecture understanding because the site is configuration- and template-driven with no API surface or code modules to document.
