{
  "name": "tokenbel-wiki",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "./build.sh",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "4.118.0"
  }
}
