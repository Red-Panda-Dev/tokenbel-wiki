# Build System

Hugo-based build toolchain for TokenBel Wiki with Docker (local) and pinned binaries (Cloudflare).

## Overview

* [Makefile](makefile.md) — Docker-based local development and build wrapper
* [Build Script](build-script.md) — Pinned Cloudflare production build
* [Tailwind Build](tailwind-build.md) — CSS compilation workflow
